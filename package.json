{
  "name": "goal-flow-agent-chat-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "GoalFlow POC tablet chat UI (talks to the cloud agent over WebSocket).",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
